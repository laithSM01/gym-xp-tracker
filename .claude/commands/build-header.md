Create a proper app header in src/layouts/AppLayout.vue with:

- App name/logo on the left: "GymXP"
- Current user's name and role badge in the middle
- Clerk UserButton on the right (it already has sign out built in)

Use Tailwind CSS, make it clean and professional. The role badge
colors should be:

- trainer = purple
- client = teal
- nutritionist = amber
